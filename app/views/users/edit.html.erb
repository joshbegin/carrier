<% provide(:title, "Edit user") %> 
<h3>Editing user</h3>

<div class="row-fluid">
  <div class="span6">
  	<%= form_for(@user, :html => { :class => "form-horizontal"} ) do |f| %>
      <%= render 'form', f: f %>
    <% end %>
  </div>
</div>

<%= link_to 'Show', @user %> |
<%= link_to 'Back', users_path %>
