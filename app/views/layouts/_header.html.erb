<% active = yield(:title) %>
<div class="navbar navbar-fixed-top">
	<div class="navbar-inner">
    <div class="container">
      <a class="btn btn-navbar navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <% if signed_in? %>
				<%= link_to "Carrier", companies_path, class: "brand" %>
			<% else %>
			  <%= link_to "Carrier", root_path, class: "brand" %>
			<% end %>
		
		
      <div class="nav-collapse">
        <ul class="nav">
          <% if signed_in? %>
            <li<%= ' class=active' if active == "Users" %>>
							<%= link_to "Users", users_path if current_user.admin? %>
						</li>
            <li id="fat-menu" class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
								Companies <b class="caret"></b>
							</a>
              <ul class="dropdown-menu">
                <li<%= ' class=active' if active == "Parent Companies" %>>
								  <%= link_to "Parent Companies", parent_companies_path %>
								</li>
                <li<%= ' class=active' if active == "Carriers" %>>
								  <%= link_to "Carriers", companies_path %>
								</li>
							</ul>
						</li>
            <li id="fat-menu" class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                    Account <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li<%= ' class=active' if active == "User details" %>>
								  <%= link_to "Profile", current_user %>
								</li>
                <li<%= ' class=active' if active == "Edit user" %>>
								  <%= link_to "Settings", edit_user_path(current_user) %>
								</li>
                <li class="divider"></li>
                <li>
                  <%= link_to "Sign out", signout_path, method: "delete" %>
                </li>
              </ul>
            </li>
          <% else %>
            <li><%= link_to "Sign in", signin_path %></li>
          <% end %>
        </ul>
			</div>
    </div>
  </div>
</div>